name: Wire Pixhub reconciliation

on:
  push:
    branches:
      - feat/minimal-pix-gateway
    paths:
      - .github/workflows/reconciliation-wire.yml

permissions:
  contents: write

jobs:
  wire:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: feat/minimal-pix-gateway
          fetch-depth: 0
      - uses: actions/setup-go@v5
        with:
          go-version: '1.24.x'
      - name: Patch source
        run: python3 .github/scripts/wire_reconciliation.py
      - name: Format
        run: gofmt -w ./cmd ./internal
      - name: Test
        run: go test ./...
      - name: Vet
        run: go vet ./...
      - name: Commit source
        run: |
          git config user.name "flashpag-bot"
          git config user.email "actions@users.noreply.github.com"
          git rm .github/workflows/reconciliation-wire.yml .github/scripts/wire_reconciliation.py
          git add internal
          git commit -m "feat: reconcile Pixhub pending and ambiguous transactions"
          git push origin HEAD:feat/minimal-pix-gateway
